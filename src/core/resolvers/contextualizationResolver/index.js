/*
 * This module handles modification on a section object according to contextualization needs
 */

export function addNote(section, content, position) {
  // find the relevant note position inside section notes
  // create object and add it
  // update numerotation
  return section;
}

export function addBlock(section, content, position) {
  return section;
}

export function mutateBlock(section, content, position) {
  return section;
}

export function mutateNote(section, content, position) {
  return section;
}
