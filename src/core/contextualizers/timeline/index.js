
export function contextualizeInlineStatic(section, contextualizer) {
  return section;
}

export function contextualizeBlockStatic(section, contextualizer) {
  return section;
}

export function contextualizeInlineDynamic(section, contextualizer) {
  return section;
}

export function contextualizeBlockDynamic(section, contextualizer) {
  return section;
}
