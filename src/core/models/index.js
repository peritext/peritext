/**
 * Models - default models used along the library
 * @module models
 */

/**
 * models of peritext sections metadata
 */
export const metadataModels = require('./metadataModels');
/**
 * models of peritext resources
 */
export const resourceModels = require('./resourceModels');
/**
 * models of possible peritext sections bibType and their inheritance rules
 */
export const sectionTypeModels = require('./sectionTypeModels');
/**
 * models of peritext contextualizers
 */
export const contextualizerModels = require('./contextualizerModels');
/**
 * models of peritext (possible, and default) rendering settings
 */
export const settingsModels = require('./settingsModels');
